const الخلاف = تتطلب ('discord.js') ؛
إعدادات const = تتطلب ('../ settings.json') ؛
export.run = async (client، message، args) => {
   عامل التصفية المستمر = (رد فعل ، مستخدم) => {
  إرجاع ["🌟"، "⚙️"]. تشمل